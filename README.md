# 🎙️ 声チェッカー (meeting-voice-check)

オンライン会議で「自分の声が相手にどう聞こえているか」をブラウザだけで録音・再生し、定量診断する単一HTMLアプリです。サーバー不要・音声は外部送信せず、すべて端末内（ブラウザ）で処理します。

## 公開URL

GitHub Pages: https://snaka.github.io/meeting-voice-check/

## 機能

- **録音・再生** — Web Audio API で生のPCMを取得。再生音は録音波形と完全一致（WAVダウンロードも可）。
- **3軸の定量診断**（各100点＋総合点）
  - **音量レベル** — 発話区間のRMS(dBFS)を理想帯（-22〜-12 dBFS）と比較
  - **ノイズ・SNR** — 無音区間のノイズフロアと発話レベルの差を算出
  - **音割れ・クリッピング** — クリップしたサンプル比率＋ピーク張り付きを検出
- **機種登録＋履歴** — 端末・マイク機種を各評価とともに `localStorage` に保存
- **機種別ランキング** — 保存データを端末×マイクで集計し平均スコア順に表示

正確に診断するため、初期状態ではブラウザの音声処理（エコー除去 / ノイズ抑制 / AGC）をオフにして生のマイク信号を測定します。会議アプリと同条件で試すトグルも用意しています。

## ローカルでの動作確認

```sh
# そのままブラウザで開く（file:// でマイクが拒否される場合あり）
open index.html

# マイクが反応しない場合はローカルサーバー経由（localhost は安全なオリジン扱い）
python3 -m http.server
# → http://localhost:8000 を開く
```

## 技術構成

- 単一の `index.html`（ビルド不要・依存なし）
- Web Audio API（getUserMedia / ScriptProcessor / AudioBuffer）
- データ保存: `localStorage`

## 将来の拡張（現状スコープ外）

「全ユーザーの機種統計を参照」する機能は、ランキング集計部分をバックエンドAPI＋DBに差し替えれば実現できる設計にしています。

## ライセンス

MIT
